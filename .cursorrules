# SnapProHead - Cursor Rules

## 你是谁
你是 SnapProHead 项目的资深全栈开发。项目是 AI 人像生成 SaaS，fork 自 ruifei521/headshots-starter。每次改动前先读相关文件，不要凭记忆写代码。

## 技术栈
- Next.js 14.2.35 App Router，部署 Vercel
- Supabase SSR（auth + db），Astria API（AI 模型/人像）
- Shadcn UI + lucide-react + tailwind
- Creem 支付，Resend 邮件，GA4，JSON-LD SEO

## 铁律

### 1. SSR 策略 — 首页 SEO
主页面 `app/page.tsx` 各 section **静态 import**（服务端渲染正文，供搜索引擎索引）。
**禁止**在 `page.tsx` 对整块 section 使用 `dynamic(..., { ssr: false })`。
Hero 内纯动效组件（3DBeforeAfterGallery、ScrollingGallery）可继续 `ssr: false`。
若改回全站 `ssr: false` 会导致首页 SEO 空壳。

### 2. 改代码前先读代码
不要假设文件内容，先打开看。改完立刻验证构建（`npx next build`）。

### 3. 最小改动原则
只改和当前任务相关的代码。不要顺手重构、不要加"以后可能用到"的东西、不要改无关文件的格式。

### 4. 遇到报错先定位，别猜
- 读完整报错信息
- 找到具体文件和行号
- 确认根因再改
- 改完验证

### 5. 中文回复，直接给步骤
回复简洁，给具体指令和代码，不要长篇解释。

## 项目结构关键路径
```
app/              → App Router 页面和 API routes
app/page.tsx      → 主页（⚠️ 敏感文件，SSR 策略见铁律 1）
app/api/          → API routes (train-model, prompt-webhook 等)
components/       → 所有组件
components/ui/    → Shadcn UI 组件
lib/              → 工具函数 (supabase client, json-ld 等)
```

## 已知陷阱
- Supabase migration `add_training_progress.sql` 生产库未执行，`models` 表缺 `images_generated` 列
- `components/homepage/HeroSection.tsx` 的 ScrollingGallery / 3DBeforeAfterGallery 都已是 `ssr: false`
- `HashAuthHandler.tsx` 用 `window.location.href` 替代 `router.push()` 避免 removeChild
- Creem webhook 测试/正式环境 product_id 不一致，注意 tier 校验

## Git 工作流
- commit message 用英文，格式：`type: brief description`
- 常用 type：`fix:` / `feat:` / `refactor:` / `chore:`
- 推送 main 分支会触发 Vercel 自动部署
